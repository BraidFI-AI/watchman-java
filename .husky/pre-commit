#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Security scans disabled for POC development
# Re-enable for production by uncommenting the line below:
# sh ./scripts/pre-commit-security.sh

echo "⚠️  Security scans (semgrep/trivy) disabled for POC development"
